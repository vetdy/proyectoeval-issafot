# proyectoeval-issasoft

## comandos
una vez bajado el backend, actualizar las dependencias 
`composer update`
reiniciar la base de datos
`php artisan migrate:fresh --seed`
iniciar el servidor
`php artisan serve`
